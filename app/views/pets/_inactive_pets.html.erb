<br />
<% unless @inactive_pets.empty? %>
  <div class="card yellow lighten-4">
    <div class="card-content">
      <span class="card-title">Inactive Pets</span>
      <ul>
        <% for pet in @inactive_pets %>
          <li><%= link_to pet.name, pet_path(pet) %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>